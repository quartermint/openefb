---
phase: 06-polish-testflight
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - efb-212/PrivacyInfo.xcprivacy
  - efb-212Tests/PrivacyManifestTests.swift
autonomous: true
requirements:
  - INFRA-04
user_setup: []

must_haves:
  truths:
    - "App includes a PrivacyInfo.xcprivacy in the bundle declaring location, audio, and UserDefaults"
    - "Privacy manifest declares no tracking and no tracking domains"
    - "PrivacyInfo.xcprivacy declares NSPrivacyAccessedAPICategoryUserDefaults with reason CA92.1"
    - "PrivacyInfo.xcprivacy declares precise location and audio data collection for app functionality"
  artifacts:
    - path: "efb-212/PrivacyInfo.xcprivacy"
      provides: "Apple-compliant privacy manifest"
      contains: "NSPrivacyCollectedDataTypePreciseLocation"
    - path: "efb-212Tests/PrivacyManifestTests.swift"
      provides: "Automated verification that privacy manifest exists and has required keys"
      contains: "NSPrivacyTracking"
  key_links:
    - from: "efb-212/PrivacyInfo.xcprivacy"
      to: "App bundle resources"
      via: "Xcode target membership (Copy Bundle Resources)"
      pattern: "PrivacyInfo.xcprivacy"
---

<objective>
Create the PrivacyInfo.xcprivacy privacy manifest required for App Store Connect / TestFlight submission. Declare all collected data types (precise location, audio), required reason APIs (UserDefaults via @AppStorage), and confirm no tracking.

Purpose: INFRA-04 blocks TestFlight distribution. Without a valid privacy manifest, App Store Connect rejects uploads with ITMS-91053.
Output: PrivacyInfo.xcprivacy at the efb-212/ project root, plus automated tests verifying the manifest contents.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-testflight/06-RESEARCH.md
@.planning/phases/06-polish-testflight/06-CONTEXT.md

@efb-212/Info.plist
@efb-212/efb_212App.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PrivacyInfo.xcprivacy privacy manifest</name>
  <files>efb-212/PrivacyInfo.xcprivacy</files>
  <read_first>
    - efb-212/Info.plist (existing background modes and usage descriptions)
    - efb-212/efb_212App.swift (app entry point structure)
    - .planning/phases/06-polish-testflight/06-RESEARCH.md (exact plist structure in Pattern 1)
  </read_first>
  <action>
Create `efb-212/PrivacyInfo.xcprivacy` as a property list file with the following exact structure:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>NSPrivacyTracking</key>
    <false/>
    <key>NSPrivacyTrackingDomains</key>
    <array/>
    <key>NSPrivacyCollectedDataTypes</key>
    <array>
        <dict>
            <key>NSPrivacyCollectedDataType</key>
            <string>NSPrivacyCollectedDataTypePreciseLocation</string>
            <key>NSPrivacyCollectedDataTypeLinked</key>
            <false/>
            <key>NSPrivacyCollectedDataTypeTracking</key>
            <false/>
            <key>NSPrivacyCollectedDataTypePurposes</key>
            <array>
                <string>NSPrivacyCollectedDataTypePurposeAppFunctionality</string>
            </array>
        </dict>
        <dict>
            <key>NSPrivacyCollectedDataType</key>
            <string>NSPrivacyCollectedDataTypeAudioData</string>
            <key>NSPrivacyCollectedDataTypeLinked</key>
            <false/>
            <key>NSPrivacyCollectedDataTypeTracking</key>
            <false/>
            <key>NSPrivacyCollectedDataTypePurposes</key>
            <array>
                <string>NSPrivacyCollectedDataTypePurposeAppFunctionality</string>
            </array>
        </dict>
    </array>
    <key>NSPrivacyAccessedAPITypes</key>
    <array>
        <dict>
            <key>NSPrivacyAccessedAPIType</key>
            <string>NSPrivacyAccessedAPICategoryUserDefaults</string>
            <key>NSPrivacyAccessedAPITypeReasons</key>
            <array>
                <string>CA92.1</string>
            </array>
        </dict>
    </array>
</dict>
</plist>
```

Key decisions:
- `NSPrivacyTracking = false`: App does no tracking (no analytics, no ad networks).
- `NSPrivacyTrackingDomains = []`: No tracking domains.
- Precise location collected: Purpose is `AppFunctionality` (in-flight GPS navigation). Not linked to identity (`Linked = false`). Not used for tracking.
- Audio data collected: Purpose is `AppFunctionality` (cockpit recording). Not linked to identity. Not used for tracking.
- UserDefaults accessed: Reason `CA92.1` (read/write to app's own UserDefaults). Required because Plan 02 will add @AppStorage for onboarding and beta banner flags.

The file must be placed inside the `efb-212/` directory (same level as `efb_212App.swift`) so Xcode's automatic file system synchronization picks it up as a target member. If Xcode does not automatically include it in "Copy Bundle Resources", manually verify target membership in the File Inspector.
  </action>
  <verify>
    <automated>grep -c "NSPrivacyTracking" efb-212/PrivacyInfo.xcprivacy && grep -c "NSPrivacyCollectedDataTypePreciseLocation" efb-212/PrivacyInfo.xcprivacy && grep -c "NSPrivacyCollectedDataTypeAudioData" efb-212/PrivacyInfo.xcprivacy && grep -c "NSPrivacyAccessedAPICategoryUserDefaults" efb-212/PrivacyInfo.xcprivacy && grep -c "CA92.1" efb-212/PrivacyInfo.xcprivacy</automated>
  </verify>
  <acceptance_criteria>
    - efb-212/PrivacyInfo.xcprivacy exists and is valid XML plist
    - File contains string "NSPrivacyTracking" with value false
    - File contains string "NSPrivacyTrackingDomains" with empty array
    - File contains string "NSPrivacyCollectedDataTypePreciseLocation"
    - File contains string "NSPrivacyCollectedDataTypeAudioData"
    - File contains string "NSPrivacyAccessedAPICategoryUserDefaults"
    - File contains string "CA92.1"
    - File contains exactly 2 entries in NSPrivacyCollectedDataTypes array
    - File contains exactly 1 entry in NSPrivacyAccessedAPITypes array
  </acceptance_criteria>
  <done>PrivacyInfo.xcprivacy exists at efb-212/PrivacyInfo.xcprivacy with all required privacy declarations for location, audio, and UserDefaults</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create privacy manifest validation tests</name>
  <files>efb-212Tests/PrivacyManifestTests.swift</files>
  <read_first>
    - efb-212/PrivacyInfo.xcprivacy (the manifest just created in Task 1)
    - efb-212Tests/efb_212Tests.swift (existing test file for test framework patterns)
    - efb-212Tests/DataTests/ (existing test directory for GRDB test patterns)
  </read_first>
  <behavior>
    - Test 1: PrivacyInfo.xcprivacy file exists in the project directory
    - Test 2: Manifest contains NSPrivacyTracking set to false
    - Test 3: Manifest contains NSPrivacyCollectedDataTypes with PreciseLocation and AudioData
    - Test 4: Manifest contains NSPrivacyAccessedAPITypes with UserDefaults category and CA92.1 reason
    - Test 5: NSPrivacyTrackingDomains is present and empty
  </behavior>
  <action>
Create `efb-212Tests/PrivacyManifestTests.swift` using Swift Testing framework (@Test, #expect) to validate the privacy manifest structure.

The test file should:
1. Load `PrivacyInfo.xcprivacy` from the project source directory using `Bundle.main` resource lookup OR by reading the file from the known relative path.
2. Parse it as a property list dictionary (`[String: Any]`).
3. Assert `NSPrivacyTracking` is `false`.
4. Assert `NSPrivacyTrackingDomains` is an empty array.
5. Assert `NSPrivacyCollectedDataTypes` array contains exactly 2 dictionaries.
6. Assert one dict has `NSPrivacyCollectedDataType == "NSPrivacyCollectedDataTypePreciseLocation"`.
7. Assert one dict has `NSPrivacyCollectedDataType == "NSPrivacyCollectedDataTypeAudioData"`.
8. Assert both collected data types have `NSPrivacyCollectedDataTypeTracking == false`.
9. Assert `NSPrivacyAccessedAPITypes` array contains exactly 1 dictionary.
10. Assert that dictionary has `NSPrivacyAccessedAPIType == "NSPrivacyAccessedAPICategoryUserDefaults"` and reasons array contains `"CA92.1"`.

Use the Swift Testing framework pattern:
```swift
import Testing
import Foundation

@Suite("Privacy Manifest Validation")
struct PrivacyManifestTests {
    // Load plist from project source directory
    // Use Bundle(for:) or file path relative to #filePath
}
```

Since the test host may not bundle PrivacyInfo.xcprivacy into the test bundle, use the file system path approach: derive the project source directory from `#filePath` (which gives the test file's compile-time path), navigate up to the project root, then read `efb-212/PrivacyInfo.xcprivacy`.
  </action>
  <verify>
    <automated>xcodebuild test -scheme efb-212 -destination 'platform=iOS Simulator,name=iPad Pro 13-inch (M4)' -only-testing:efb-212Tests/PrivacyManifestTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - efb-212Tests/PrivacyManifestTests.swift exists
    - File contains "import Testing"
    - File contains "@Suite" or "@Test"
    - File contains "NSPrivacyTracking"
    - File contains "NSPrivacyCollectedDataTypePreciseLocation"
    - File contains "NSPrivacyAccessedAPICategoryUserDefaults"
    - File contains "CA92.1"
    - Tests pass when run against the created PrivacyInfo.xcprivacy
  </acceptance_criteria>
  <done>Automated tests validate that PrivacyInfo.xcprivacy contains all required Apple privacy declarations; tests pass green</done>
</task>

</tasks>

<verification>
1. `efb-212/PrivacyInfo.xcprivacy` exists and is valid XML
2. Privacy manifest tests pass: `xcodebuild test -scheme efb-212 -destination 'platform=iOS Simulator,name=iPad Pro 13-inch (M4)' -only-testing:efb-212Tests/PrivacyManifestTests`
3. `grep "NSPrivacyCollectedDataTypePreciseLocation" efb-212/PrivacyInfo.xcprivacy` returns a match
4. `grep "NSPrivacyCollectedDataTypeAudioData" efb-212/PrivacyInfo.xcprivacy` returns a match
5. `grep "CA92.1" efb-212/PrivacyInfo.xcprivacy` returns a match
</verification>

<success_criteria>
- PrivacyInfo.xcprivacy in efb-212/ declares location + audio data collection and UserDefaults API usage
- No tracking declared (NSPrivacyTracking = false)
- Automated tests verify all manifest contents
- File is in the correct location for Xcode automatic target membership
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-testflight/06-01-SUMMARY.md`
</output>
