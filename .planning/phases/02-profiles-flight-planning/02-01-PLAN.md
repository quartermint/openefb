---
phase: 02-profiles-flight-planning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - efb-212/Data/Models/UserSettings.swift
  - efb-212/Data/Models/AircraftProfile.swift
  - efb-212/Data/Models/PilotProfile.swift
  - efb-212/Data/Models/FlightPlanRecord.swift
  - efb-212/efb_212App.swift
  - efb-212/Core/AppState.swift
  - efb-212/Core/Types.swift
  - efb-212/Services/CurrencyService.swift
  - efb-212Tests/ServiceTests/CurrencyServiceTests.swift
  - efb-212Tests/DataTests/ProfileModelTests.swift
autonomous: true
requirements:
  - PLAN-03
  - PLAN-04

must_haves:
  truths:
    - "SwiftData models for AircraftProfile, PilotProfile, FlightPlanRecord exist inside SchemaV1 enum"
    - "App launches without crash after ModelContainer registers all four model types"
    - "CurrencyService correctly computes medical expiry, flight review, and night currency per FAR 61.23/61.56/61.57"
    - "AppState holds activeAircraftProfileID and activePilotProfileID as persisted identifiers"
  artifacts:
    - path: "efb-212/Data/Models/AircraftProfile.swift"
      provides: "SchemaV1.AircraftProfile @Model class"
      contains: "@Model"
    - path: "efb-212/Data/Models/PilotProfile.swift"
      provides: "SchemaV1.PilotProfile @Model class"
      contains: "@Model"
    - path: "efb-212/Data/Models/FlightPlanRecord.swift"
      provides: "SchemaV1.FlightPlanRecord @Model class"
      contains: "@Model"
    - path: "efb-212/Services/CurrencyService.swift"
      provides: "FAR currency computation logic"
      contains: "CurrencyService"
    - path: "efb-212Tests/ServiceTests/CurrencyServiceTests.swift"
      provides: "Currency computation unit tests"
      contains: "CurrencyServiceTests"
  key_links:
    - from: "efb-212/efb_212App.swift"
      to: "SchemaV1"
      via: "modelContainer(for:) registering all 4 models"
      pattern: "modelContainer.*AircraftProfile.*PilotProfile.*FlightPlanRecord"
    - from: "efb-212/Core/AppState.swift"
      to: "profile selection"
      via: "activeAircraftProfileID and activePilotProfileID stored properties"
      pattern: "activeAircraftProfileID|activePilotProfileID"
---

<objective>
Create the SwiftData foundation for Phase 2: AircraftProfile, PilotProfile, and FlightPlanRecord models inside SchemaV1, CurrencyService with FAR 61.23/61.56/61.57 logic, AppState profile selection properties, and unit tests.

Purpose: Every other plan in Phase 2 depends on these models and the currency computation. This is the data layer and business logic foundation.
Output: Three new @Model classes registered in ModelContainer, CurrencyService with tested currency computation, AppState extended with profile selection state.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-profiles-flight-planning/02-CONTEXT.md
@.planning/phases/02-profiles-flight-planning/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From efb-212/Data/Models/UserSettings.swift (SchemaV1 pattern to extend):
```swift
enum SchemaV1: VersionedSchema {
    static var versionIdentifier = Schema.Version(1, 0, 0)
    static var models: [any PersistentModel.Type] { [UserSettings.self] }

    @Model
    final class UserSettings {
        var defaultMapMode: String = "northUp"
        var sectionalOpacity: Double = 0.70
        var weatherRefreshInterval: Double = 900
        var createdAt: Date = Date()
        init() {}
    }
}

enum EFBSchemaMigrationPlan: SchemaMigrationPlan {
    static var schemas: [any VersionedSchema.Type] { [SchemaV1.self] }
    static var stages: [MigrationStage] { [] }
}
```

From efb-212/efb_212App.swift (ModelContainer registration):
```swift
@main
struct efb_212App: App {
    @State private var appState = AppState()
    var body: some Scene {
        WindowGroup {
            ContentView()
                .environment(appState)
                .modelContainer(for: SchemaV1.UserSettings.self)
        }
    }
}
```

From efb-212/Core/AppState.swift (state coordinator to extend):
```swift
@Observable
@MainActor
final class AppState {
    // ... existing Navigation, Map, Location, Flight Plan, System state sections
    var activeFlightPlan: Bool = false
    var distanceToNext: Double?
    var estimatedTimeEnroute: TimeInterval?
    var directToAirport: Airport?
}
```

From efb-212/Core/Types.swift (existing types to use):
```swift
struct VSpeeds: Codable, Equatable, Sendable {
    var vr: Int?; var vx: Int?; var vy: Int?; var va: Int?
    var vne: Int?; var vfe: Int?; var vs0: Int?; var vs1: Int?
}

enum MedicalClass: String, Codable, CaseIterable, Sendable {
    case first, second, third, basicMed
}

enum CertificateType: String, Codable, CaseIterable, Sendable {
    case student, sport, recreational, privatePilot = "private", commercial, atp
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SwiftData models + AppState extension + CurrencyService</name>
  <files>
    efb-212/Data/Models/AircraftProfile.swift,
    efb-212/Data/Models/PilotProfile.swift,
    efb-212/Data/Models/FlightPlanRecord.swift,
    efb-212/Data/Models/UserSettings.swift,
    efb-212/efb_212App.swift,
    efb-212/Core/AppState.swift,
    efb-212/Core/Types.swift,
    efb-212/Services/CurrencyService.swift
  </files>
  <read_first>
    efb-212/Data/Models/UserSettings.swift,
    efb-212/efb_212App.swift,
    efb-212/Core/AppState.swift,
    efb-212/Core/Types.swift,
    efb-212/Core/AviationModels.swift
  </read_first>
  <behavior>
    - CurrencyService.medicalStatus(expiryDate: Date 60 days from now) -> .current
    - CurrencyService.medicalStatus(expiryDate: Date 15 days from now) -> .warning
    - CurrencyService.medicalStatus(expiryDate: Date 1 day ago) -> .expired
    - CurrencyService.medicalStatus(expiryDate: nil) -> .expired
    - CurrencyService.flightReviewStatus(reviewDate: Date 12 months ago) -> .current
    - CurrencyService.flightReviewStatus(reviewDate: Date 23 months ago) -> .warning (1 month remaining)
    - CurrencyService.flightReviewStatus(reviewDate: Date 25 months ago) -> .expired
    - CurrencyService.flightReviewStatus(reviewDate: nil) -> .expired
    - CurrencyService.nightCurrencyStatus(nightLandings: [(30 days ago, 3)]) -> .current
    - CurrencyService.nightCurrencyStatus(nightLandings: [(30 days ago, 2)]) -> .expired (only 2, need 3)
    - CurrencyService.nightCurrencyStatus(nightLandings: [(100 days ago, 5)]) -> .expired (outside 90-day window)
    - CurrencyService.nightCurrencyStatus(nightLandings: []) -> .expired
    - CurrencyService.overallStatus(medical: .current, flightReview: .current, night: .current) -> .current
    - CurrencyService.overallStatus(medical: .current, flightReview: .warning, night: .current) -> .warning
    - CurrencyService.overallStatus(medical: .expired, flightReview: .current, night: .current) -> .expired
  </behavior>
  <action>
    **1. Add CurrencyStatus enum to Types.swift:**
    Add after the existing `CertificateType` enum:
    ```swift
    enum CurrencyStatus: String, Codable, CaseIterable, Sendable {
        case current    // green: > 30 days remaining
        case warning    // yellow: <= 30 days remaining
        case expired    // red: past expiration
    }
    ```

    **2. Create efb-212/Data/Models/AircraftProfile.swift:**
    Define `SchemaV1.AircraftProfile` as a `@Model final class` INSIDE an `extension SchemaV1` block (NOT in UserSettings.swift -- separate file, same enum). Fields:
    - `var id: UUID = UUID()`
    - `var nNumber: String = ""` (FAA registration, e.g., "N4543A")
    - `var aircraftType: String = ""` (free text, e.g., "Cessna 172SP")
    - `var fuelCapacityGallons: Double?` (usable fuel -- gallons)
    - `var fuelBurnGPH: Double?` (cruise fuel burn -- gallons per hour)
    - `var cruiseSpeedKts: Double?` (cruise TAS -- knots)
    - `var vSpeedsData: Data?` (JSON-encoded VSpeeds struct, optional)
    - `var isActive: Bool = false` (currently selected aircraft)
    - `var annualDue: Date?`
    - `var transponderDue: Date?`
    - `var createdAt: Date = Date()`
    - `init(nNumber: String)` that sets self.nNumber = nNumber

    Add computed property `var vSpeeds: VSpeeds?` with get/set that encodes/decodes `vSpeedsData` via JSONEncoder/JSONDecoder.

    Add computed property `var isValid: Bool` that returns `!nNumber.isEmpty`.

    **3. Create efb-212/Data/Models/PilotProfile.swift:**
    Define `SchemaV1.PilotProfile` as a `@Model final class` inside `extension SchemaV1`. Fields:
    - `var id: UUID = UUID()`
    - `var name: String = ""`
    - `var certificateNumber: String?`
    - `var certificateType: String?` (CertificateType.rawValue)
    - `var medicalClass: String?` (MedicalClass.rawValue)
    - `var medicalExpiry: Date?`
    - `var flightReviewDate: Date?`
    - `var totalHours: Double?`
    - `var isActive: Bool = false` (currently selected pilot)
    - `var nightLandingEntriesData: Data?` (JSON-encoded array of NightLandingEntry)
    - `var createdAt: Date = Date()`
    - `init() {}`

    Add nested struct `NightLandingEntry: Codable, Equatable, Sendable` with `var date: Date` and `var count: Int`.

    Add computed property `var nightLandingEntries: [NightLandingEntry]` with get/set that encodes/decodes `nightLandingEntriesData`.

    Add computed `var medicalClassEnum: MedicalClass?` get/set that maps to/from `medicalClass` string.
    Add computed `var certificateTypeEnum: CertificateType?` get/set that maps to/from `certificateType` string.

    **4. Create efb-212/Data/Models/FlightPlanRecord.swift:**
    Define `SchemaV1.FlightPlanRecord` as a `@Model final class` inside `extension SchemaV1`. Fields:
    - `var id: UUID = UUID()`
    - `var name: String?`
    - `var departureICAO: String = ""`
    - `var departureName: String = ""`
    - `var departureLatitude: Double = 0`
    - `var departureLongitude: Double = 0`
    - `var destinationICAO: String = ""`
    - `var destinationName: String = ""`
    - `var destinationLatitude: Double = 0`
    - `var destinationLongitude: Double = 0`
    - `var cruiseAltitude: Int = 3000` (feet MSL)
    - `var cruiseSpeedKts: Double = 100` (knots TAS)
    - `var fuelBurnGPH: Double?` (gallons per hour)
    - `var totalDistanceNM: Double = 0` (nautical miles)
    - `var estimatedTimeSeconds: Double = 0` (seconds)
    - `var estimatedFuelGallons: Double?`
    - `var notes: String?`
    - `var lastUsedAt: Date = Date()`
    - `var createdAt: Date = Date()`
    - `init() {}`

    Add method `func toFlightPlan() -> FlightPlan` that converts to the existing `FlightPlan` struct from AviationModels.swift. Populate departure/destination as Waypoints.

    Add `convenience init(from plan: FlightPlan, departureName: String, destinationName: String)` that fills fields from a FlightPlan struct.

    **5. Update efb-212/Data/Models/UserSettings.swift:**
    Update the `models` array in `SchemaV1` to register all four model types:
    ```swift
    static var models: [any PersistentModel.Type] {
        [UserSettings.self, AircraftProfile.self, PilotProfile.self, FlightPlanRecord.self]
    }
    ```
    Also add to UserSettings model:
    - `var activeAircraftID: String?` (UUID string of active aircraft, persists across launches)
    - `var activePilotID: String?` (UUID string of active pilot, persists across launches)
    - `var lastFlightPlanID: String?` (UUID string of last used flight plan, for auto-load)

    **6. Update efb-212/efb_212App.swift:**
    Change the modelContainer registration to use an array of all model types:
    ```swift
    .modelContainer(for: [
        SchemaV1.UserSettings.self,
        SchemaV1.AircraftProfile.self,
        SchemaV1.PilotProfile.self,
        SchemaV1.FlightPlanRecord.self
    ])
    ```

    **7. Update efb-212/Core/AppState.swift:**
    Add a new `// MARK: - Profile State` section after the Flight Plan State section:
    ```swift
    // MARK: - Profile State
    var activeAircraftProfileID: UUID?
    var activePilotProfileID: UUID?
    ```

    **8. Create efb-212/Services/CurrencyService.swift:**
    A `struct CurrencyService` (not actor, not class -- pure functions) with `static` methods:
    - `static func medicalStatus(expiryDate: Date?) -> CurrencyStatus` -- nil returns .expired, past date returns .expired, <=30 days returns .warning, >30 days returns .current. Uses `Calendar.current.dateComponents([.day], from: now, to: expiry).day`.
    - `static func flightReviewStatus(reviewDate: Date?) -> CurrencyStatus` -- computes expiry as `Calendar.current.date(byAdding: .month, value: 24, to: reviewDate)`, then delegates to medicalStatus logic.
    - `static func nightCurrencyStatus(nightLandings: [(date: Date, count: Int)]) -> CurrencyStatus` -- filters landings within past 90 days (`Calendar.current.date(byAdding: .day, value: -90, to: Date())`), sums counts, returns .current if >= 3, else .expired. No .warning state for 61.57.
    - `static func overallStatus(medical: CurrencyStatus, flightReview: CurrencyStatus, night: CurrencyStatus) -> CurrencyStatus` -- returns .expired if any is .expired, .warning if any is .warning, else .current.
  </action>
  <verify>
    <automated>xcodebuild build -scheme efb-212 -destination 'platform=iOS Simulator,name=iPad Pro 13-inch (M4)' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - efb-212/Data/Models/AircraftProfile.swift contains `@Model` and `final class AircraftProfile`
    - efb-212/Data/Models/PilotProfile.swift contains `@Model` and `final class PilotProfile`
    - efb-212/Data/Models/FlightPlanRecord.swift contains `@Model` and `final class FlightPlanRecord`
    - efb-212/Data/Models/UserSettings.swift models array contains `AircraftProfile.self, PilotProfile.self, FlightPlanRecord.self`
    - efb-212/efb_212App.swift contains `SchemaV1.AircraftProfile.self` in modelContainer call
    - efb-212/Core/AppState.swift contains `var activeAircraftProfileID: UUID?`
    - efb-212/Core/Types.swift contains `enum CurrencyStatus`
    - efb-212/Services/CurrencyService.swift contains `struct CurrencyService` and `static func medicalStatus`
    - Build succeeds with no errors
  </acceptance_criteria>
  <done>All four SwiftData models registered in SchemaV1 and ModelContainer, CurrencyService computes medical/flight review/night currency correctly, AppState has profile selection properties, app builds and launches without crash.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CurrencyService tests + ProfileModel tests</name>
  <files>
    efb-212Tests/ServiceTests/CurrencyServiceTests.swift,
    efb-212Tests/DataTests/ProfileModelTests.swift
  </files>
  <read_first>
    efb-212/Services/CurrencyService.swift,
    efb-212/Data/Models/AircraftProfile.swift,
    efb-212/Data/Models/PilotProfile.swift,
    efb-212/Data/Models/FlightPlanRecord.swift,
    efb-212/Core/Types.swift,
    efb-212Tests/ServiceTests/WeatherServiceTests.swift
  </read_first>
  <behavior>
    - Medical: 60 days out -> .current, 15 days out -> .warning, yesterday -> .expired, nil -> .expired
    - Flight review: 12 months ago -> .current (12 months left), 23 months ago -> .warning (1 month left), 25 months ago -> .expired
    - Night: 3 landings in 30 days -> .current, 2 landings in 30 days -> .expired, 5 landings 100 days ago -> .expired
    - Overall: all current -> .current, one warning -> .warning, one expired -> .expired
    - AircraftProfile: can set/get vSpeeds via computed property, isValid returns false for empty nNumber
    - PilotProfile: can set/get nightLandingEntries via computed property, enum computed properties round-trip correctly
    - FlightPlanRecord: toFlightPlan() produces correct FlightPlan struct
  </behavior>
  <action>
    **1. Create efb-212Tests/ServiceTests/CurrencyServiceTests.swift:**
    Using Swift Testing framework (`import Testing`), create `@Suite("CurrencyService Tests") struct CurrencyServiceTests`.

    Test cases for medicalStatus:
    - `@Test func medicalCurrentWhenMoreThan30Days()` -- set expiryDate to 60 days from now, expect .current
    - `@Test func medicalWarningWhenWithin30Days()` -- set expiryDate to 15 days from now, expect .warning
    - `@Test func medicalExpiredWhenPastDate()` -- set expiryDate to yesterday, expect .expired
    - `@Test func medicalExpiredWhenNil()` -- pass nil, expect .expired
    - `@Test func medicalWarningAtExactly30Days()` -- set expiryDate to exactly 30 days from now, expect .warning

    Test cases for flightReviewStatus:
    - `@Test func flightReviewCurrentWhenRecent()` -- set reviewDate to 12 months ago, expect .current
    - `@Test func flightReviewWarningWhenApproaching()` -- set reviewDate to 23 months ago (~1 month remaining), expect .warning
    - `@Test func flightReviewExpiredWhenPast24Months()` -- set reviewDate to 25 months ago, expect .expired
    - `@Test func flightReviewExpiredWhenNil()` -- pass nil, expect .expired

    Test cases for nightCurrencyStatus:
    - `@Test func nightCurrentWith3Landings()` -- 3 landings 30 days ago, expect .current
    - `@Test func nightExpiredWith2Landings()` -- 2 landings 30 days ago, expect .expired
    - `@Test func nightExpiredWhenOutside90Days()` -- 5 landings 100 days ago, expect .expired
    - `@Test func nightExpiredWhenEmpty()` -- empty array, expect .expired
    - `@Test func nightCurrentWithMultipleEntries()` -- [(45 days ago, 1), (30 days ago, 1), (10 days ago, 1)], expect .current (total 3)

    Test cases for overallStatus:
    - `@Test func overallCurrentWhenAllCurrent()` -- expect .current
    - `@Test func overallWarningWhenOneWarning()` -- medical .warning, rest .current, expect .warning
    - `@Test func overallExpiredWhenOneExpired()` -- night .expired, rest .current, expect .expired

    Use `Calendar.current.date(byAdding:value:to:)` to create test dates relative to `Date()`.

    **2. Create efb-212Tests/DataTests/ProfileModelTests.swift:**
    Using Swift Testing framework, create `@Suite("Profile Model Tests") struct ProfileModelTests`.

    AircraftProfile tests:
    - `@Test func aircraftProfileCreation()` -- create with nNumber "N4543A", verify nNumber is set, id is non-nil, isActive is false
    - `@Test func aircraftVSpeedsRoundTrip()` -- set vSpeeds computed property to VSpeeds(vr: 55, vy: 76), read back, verify values match
    - `@Test func aircraftIsValidWithNNumber()` -- create with "N123AB", verify isValid is true
    - `@Test func aircraftIsInvalidWithEmptyNNumber()` -- create with "", verify isValid is false

    PilotProfile tests:
    - `@Test func pilotProfileCreation()` -- create, set name to "Test Pilot", verify name persists
    - `@Test func pilotNightLandingsRoundTrip()` -- set nightLandingEntries to [NightLandingEntry(date: Date(), count: 3)], read back, verify count is 3
    - `@Test func pilotMedicalClassEnumRoundTrip()` -- set medicalClassEnum to .first, read back, verify .first
    - `@Test func pilotCertificateTypeEnumRoundTrip()` -- set certificateTypeEnum to .privatePilot, read back, verify .privatePilot

    FlightPlanRecord tests:
    - `@Test func flightPlanRecordToFlightPlan()` -- create record with departureICAO "KPAO", destinationICAO "KSQL", totalDistanceNM 5.3, call toFlightPlan(), verify departure == "KPAO" and totalDistance == 5.3
  </action>
  <verify>
    <automated>xcodebuild test -scheme efb-212 -destination 'platform=iOS Simulator,name=iPad Pro 13-inch (M4)' -only-testing:efb-212Tests/CurrencyServiceTests -only-testing:efb-212Tests/ProfileModelTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - efb-212Tests/ServiceTests/CurrencyServiceTests.swift contains `@Suite("CurrencyService Tests")`
    - efb-212Tests/ServiceTests/CurrencyServiceTests.swift contains `medicalCurrentWhenMoreThan30Days` and `nightExpiredWhenEmpty`
    - efb-212Tests/DataTests/ProfileModelTests.swift contains `@Suite("Profile Model Tests")`
    - efb-212Tests/DataTests/ProfileModelTests.swift contains `aircraftVSpeedsRoundTrip` and `flightPlanRecordToFlightPlan`
    - All currency service tests pass
    - All profile model tests pass
  </acceptance_criteria>
  <done>CurrencyService has comprehensive unit tests covering all three currency types plus overall status. Profile models have creation, computed property round-trip, and validation tests. All tests green.</done>
</task>

</tasks>

<verification>
- `xcodebuild build -scheme efb-212 -destination 'platform=iOS Simulator,name=iPad Pro 13-inch (M4)'` succeeds
- `xcodebuild test -scheme efb-212 -destination 'platform=iOS Simulator,name=iPad Pro 13-inch (M4)' -only-testing:efb-212Tests/CurrencyServiceTests -only-testing:efb-212Tests/ProfileModelTests` all green
- App launches in simulator without crash (ModelContainer registers all 4 models)
</verification>

<success_criteria>
- Four SwiftData @Model classes (UserSettings, AircraftProfile, PilotProfile, FlightPlanRecord) registered in SchemaV1
- ModelContainer in efb_212App.swift includes all four model types
- CurrencyService computes medical (FAR 61.23), flight review (FAR 61.56), and night currency (FAR 61.57) with correct thresholds
- AppState has activeAircraftProfileID and activePilotProfileID properties
- 15+ unit tests covering currency computation and model behavior, all green
</success_criteria>

<output>
After completion, create `.planning/phases/02-profiles-flight-planning/02-01-SUMMARY.md`
</output>
